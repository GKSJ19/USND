from dashboard import server as app

# This file acts as the primary entry point for Vercel's Python runtime.
# It imports the underlying Flask `server` from your dash application
# and exposes it as `app`, which Vercel expects.